apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: dev
  namespace: echo-server
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: echo-server
      sources:
        direct: true
  verification:
    analysisTemplates:
      - name: echo-server-dev-verify
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/AaronRoethe/homelab
            checkout:
              - branch: main
                path: ./src
        - uses: yaml-update
          as: update-image
          config:
            path: ./src/platform/apps/demo/dev/echo-server.yaml
            updates:
              - key: spec.source.helm.valuesObject.image.tag
                value: ${{ imageFrom("registry.infra.svc.cluster.local:5000/echo-server").Tag }}
        - uses: git-commit
          config:
            path: ./src
            messageFromSteps:
              - update-image
        - uses: git-push
          config:
            path: ./src
        - uses: argocd-update
          config:
            apps:
              - name: echo-server-dev
                sources:
                  - repoURL: https://github.com/AaronRoethe/homelab
                    desiredRevision: ${{ outputs.steps['git-push'].commit }}
