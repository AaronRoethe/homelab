apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: echo-server-dev
  namespace: argocd
  labels:
    homelab/tier: app
    homelab/app: echo-server
    homelab/env: dev
    kargo.akuity.io/authorized-stage: echo-server:dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Base chart from app repo
    - repoURL: https://github.com/aroethe/homelab
      targetRevision: main
      path: apps/echo-server/chart
      helm:
        valueFiles:
          - $overlays/platform/overlays/echo-server/values-pi.yaml
          - $overlays/platform/overlays/echo-server/values-dev.yaml
    # Overlay values from platform
    - repoURL: https://github.com/aroethe/homelab
      targetRevision: main
      ref: overlays
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
